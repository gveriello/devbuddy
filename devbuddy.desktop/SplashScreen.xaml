<Window x:Class="devbuddy.Desktop.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:devbuddy.Desktop"
        xmlns:userControls="clr-namespace:devbuddy.Desktop.UserControls"
        xmlns:windows="clr-namespace:devbuddy.Desktop.Windows"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Height="410" Width="600"
        MinHeight="400" MinWidth="600"
        MaxHeight="400" MaxWidth="600">

    <Grid>
        <Grid HorizontalAlignment="Stretch" 
              VerticalAlignment="Stretch"
              Margin="30,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <userControls:Logo Grid.Row="1" Grid.Column="0" 
                                  Width="400" Height="150"
                                  HorizontalAlignment="Left"/>

            
        </Grid>
    </Grid>
</Window>